# rigmarole
scale a rig from a configuration file and animate it with trajectory IK

import_rig.py currently has the import of IK empties 

add_ik.py  sets the ik constraints

rigmarole.py does detailed segment scaling. Not currently linked to the config script